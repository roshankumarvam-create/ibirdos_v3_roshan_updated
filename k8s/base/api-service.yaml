apiVersion: v1
kind: Service
metadata: { name: api, labels: { app: api } }
spec:
  type: ClusterIP
  selector: { app: api }
  ports:
    - { name: http, port: 3001, targetPort: http }
    - { name: ws, port: 3002, targetPort: ws }
